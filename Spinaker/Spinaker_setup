git config --global user.email \
    "pucenty@example.com"
git config --global user.name \
    "Pucenty"

export PROJECT_ID=polar-automata-405407
gcloud config set project $PROJECT_ID
gcloud config list

~/cloudshell_open/spinnaker-for-gcp/scripts/install/setup_properties.sh